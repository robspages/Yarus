<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :user_id %>
  <div class="field">
    <%= f.text_field :fullurl, :class=>'round'  %>
  </div>
  <div class="actions">
    <%= f.submit "Shorten that puppy", :class=>'btn btn-large btn-block btn-success' %>
  </div>
<% end %>
